# Legacy Files

This directory contains legacy files from the refactoring process.

## Files

- `rag_store.py` - Old vector store implementation (replaced by `core/vectorstore.py`)
- `vectorstore_oracle.py` - Old Oracle vector store (replaced by `core/vectorstore.py`)
- `app_old.py` - Original FastAPI app before refactoring

## Note

These files are kept for reference only. All functionality has been migrated to the new structure.

**Do not use these files in production.**
